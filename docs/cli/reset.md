---
summary: "CLI reference for `shehzadalgo reset` (reset local state/config)"
read_when:
  - You want to wipe local state while keeping the CLI installed
  - You want a dry-run of what would be removed
title: "reset"
---

# `shehzadalgo reset`

Reset local config/state (keeps the CLI installed).

```bash
shehzadalgo reset
shehzadalgo reset --dry-run
shehzadalgo reset --scope config+creds+sessions --yes --non-interactive
```
