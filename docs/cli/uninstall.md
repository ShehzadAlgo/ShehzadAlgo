---
summary: "CLI reference for `shehzadalgo uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `shehzadalgo uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
shehzadalgo uninstall
shehzadalgo uninstall --all --yes
shehzadalgo uninstall --dry-run
```
